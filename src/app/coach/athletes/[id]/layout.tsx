import { AthleteWorkspaceLayout } from "@/components/coach/athletes/AthleteWorkspaceLayout";

export default function AthleteWorkspaceLayoutWrapper({ children }: { children: React.ReactNode }) {
  return <AthleteWorkspaceLayout>{children}</AthleteWorkspaceLayout>;
}
